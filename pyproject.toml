[project]
name = "animeBot"
version = "0.1.0"
description = "Discord bot for anime release notifications"
requires-python = ">=3.12"

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I"]